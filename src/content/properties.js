// Section-level metadata only. Listings themselves now live in Supabase
// and are fetched at runtime via `src/lib/listings.js`. See
// `src/components/FeaturedProperties.jsx` for the integration.
export const properties = {
  eyebrow: 'Featured properties',
  viewAllLabel: 'View all properties',
  viewAllTo: '/selling',
}
